services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docker-lab-app
    ports:
      - "8080:8080"
    networks:
      - lab-network
    restart: unless-stopped

networks:
  lab-network:
    driver: bridge
